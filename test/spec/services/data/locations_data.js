'use strict';

describe('Service: locationsData', function () {

  // load the service's module
  beforeEach(module('arkhamHelperApp'));

  // instantiate service
  var locationsData;
  beforeEach(inject(function (_locationsData_) {
    locationsData = _locationsData_;
  }));

  it('should do something');

});
